version: '3'

services:
    monitor:
      build: ".."
      image: "rocknrollcaller"
      restart: "unless-stopped"
      volumes:
        - "./example_monitoring_configuration.yml:/monitoring_configuration.yml:Z"